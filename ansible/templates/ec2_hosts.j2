---
all:
  children: 
    aws_instance:
      hosts:
      {% for ec2 in awx_instance.instances %}
        {{ ec2.public_ip}}:
      {% endfor %}
  vars:
    ansible_ssh_user: ec2-user
    common_tools:
